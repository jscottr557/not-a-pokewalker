#include "invalid.h"

//TODO: implement
// 			will need sound and screen control utilities set up first
void v_invalid() {
	//check device settings
	//if the volume is up, play some sort of err beep
	//else do something to the screen... can i "save" its state, flash it, and then redraw?
}